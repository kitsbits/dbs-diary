 // ACTIONS \\
import {getList, addShit, deleteShit, editShit} from "./shitlist";
import {saveEntry, startEntry, deleteEntry, getEntries} from "./journal";

export {
    getList,
    addShit,
    deleteShit,
    editShit,
    saveEntry,
    startEntry,
    deleteEntry,
    getEntries
}
